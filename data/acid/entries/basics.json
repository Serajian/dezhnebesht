[
  {
    "id": "db-transaction",
    "tags": [
      "transaction",
      "structure"
    ],
    "related": [
      "transaction-boundary",
      "autocommit",
      "acid",
      "transaction"
    ],
    "fa": {
      "title": "تراکنش پایگاه داده",
      "short": "چند دستور که یک واحد می‌شوند — و تضمین اصلی این نیست که هر دو انجام می‌شوند، این است که بیرون هیچ‌وقت فقط یکی‌شان را نمی‌بیند",
      "body": "<p>صد هزار تومان از حساب علی به حساب سارا. برای پایگاه داده این یک کار نیست، دو کار است: یک <code>UPDATE</code> که از علی کم می‌کند و یک <code>UPDATE</code> که به سارا اضافه می‌کند.</p><p>روایت آشنا اینجا می‌گوید: اگر بین این دو برق برود، پول بخار می‌شود. درست است و مهم است — ولی کوچک‌ترین بخش ماجراست، چون قطع برق نادر است.</p><p>چیزی که هر ثانیه در حال رخ دادن است این است: بین آن دو دستور، برای مدتی که با میلی‌ثانیه اندازه می‌گیرند، صد هزار تومان در هیچ حسابی نیست. اگر گزارش مانده کل دقیقاً همان لحظه اجرا شود، عددی می‌گیرد که هیچ‌وقت درست نبوده و هیچ‌وقت هم دوباره دیده نمی‌شود. نه خطایی هست، نه سطری در لاگ، و نه راهی برای بازتولیدش.</p><p><strong>تراکنش پایگاه داده</strong> <span dir=\"ltr\">(Database Transaction)</span> پاسخ به هر دوی این‌هاست: چند دستور را در یک واحد می‌بندد و آن واحد از بیرون یک قدم دیده می‌شود، نه چند قدم. یا هر دو <code>UPDATE</code> رخ داده‌اند یا هیچ‌کدام، و لحظه‌ای وجود ندارد که خواننده‌ای از بیرون یکی را بدون دیگری ببیند.</p><p><strong>و همین بند دوم است که از تعریف‌ها جا می‌افتد.</strong></p><p>«یا همه یا هیچ» جمله‌ای دربارهٔ نتیجه است: وقتی کار تمام شد، یکی از دو حالت برقرار است. ولی خواننده‌ای که وسط کار می‌پرسد نتیجه را نمی‌بیند؛ همان لحظه را می‌بیند. تضمین دوم دربارهٔ همان لحظه است، و بخش مثال دقیقاً همین را نشان می‌دهد: نشستی که وسط انتقال می‌پرسد، حالت نصفه را نمی‌گیرد — حالت قبل را می‌گیرد، دست‌نخورده و درست.</p><p>این دو تضمین نام‌های جدا دارند و مرحله‌های بعدی این موضوع هرکدام را جداگانه باز می‌کنند. آنچه از اینجا باید برداشت این است که دوتا هستند، نه یکی — و کسی که فقط اولی را شنیده باشد، نصف کاری را که تراکنش برایش می‌کند نمی‌داند.</p><p><strong>و اینکه این واحد چطور اعلام می‌شود.</strong></p><p>با سه دستور. <code>BEGIN</code> واحد را باز می‌کند، <code>COMMIT</code> می‌گوید «تمام شد، ثبتش کن»، و <code>ROLLBACK</code> می‌گوید «منصرف شدم، انگار هیچ‌کدامش نبود». تا وقتی <code>COMMIT</code> نیامده، تغییرها واقعاً وجود دارند و خودِ نشست می‌بیندشان — ولی برای هیچ‌کس دیگری وجود ندارند.</p><p>کاری که <code>COMMIT</code> می‌کند در فارسیِ این سایت <strong>تعهد</strong> <span dir=\"ltr\">(Commit)</span> نامیده می‌شود، نه «کامیت». کامیت روی این سایت کامیت گیت است و این دو یکی نیستند؛ نگه داشتن این تفکیک از همین‌جا ارزان‌تر است تا بعد از چهل مدخل.</p><p><strong>و یک سوءتفاهم که زود باید برداشته شود: تراکنش قفل نیست.</strong></p><p>باز کردن تراکنش به‌خودی‌خود هیچ‌چیز را برای دیگران نمی‌بندد. کارهایی که <em>داخلش</em> می‌کنی ممکن است قفل بگیرند، ولی <code>BEGIN</code> خودش هیچ قفلی نمی‌گیرد و هیچ نشست دیگری را منتظر نمی‌گذارد. در بخش مثال، نشست دوم وسط تراکنش نشست اول بی‌هیچ درنگی پاسخ می‌گیرد: نه مسدود می‌شود، نه خطا می‌بیند، و نه اصلاً خبر دارد که تراکنشی باز است.</p><p><strong>و یک تداخل نام که خواننده باید بداند.</strong></p><p>روی این سایت شناسهٔ <span dir=\"ltr\">transaction</span> از قبل صاحب دارد: تراکنش بیت‌کوین، در موضوع کریپتو. یک واژه و دو چیز کاملاً متفاوت — آنجا پیامی امضاشده است که به شبکه فرستاده می‌شود و «فرستادم» یعنی «شروع شد»؛ اینجا واحدی از کار است داخل یک موتور، که «فرستادم» در آن اصلاً معنایی ندارد. به همین دلیل شناسهٔ این یکی <span dir=\"ltr\">db-transaction</span> است. آن یکی را پایین همین صفحه، میان اصطلاح‌های مرتبط، پیدا می‌کنی.</p><p><strong>و آخرین چیزی که در بازبینی جواب می‌دهد.</strong></p><p>هرجا در کدی دیدی دو دستور نوشتن پشت سر هم آمده‌اند، یک پرسش بپرس: اگر برنامه دقیقاً بین این دو بمیرد، پایگاه داده در چه حالتی می‌ماند و چه کسی آن حالت را می‌بیند؟ اگر پاسخ «نمی‌دانم» بود، آن دو دستور هنوز یک واحد نشده‌اند — و اینکه تا امروز چیزی خراب نشده، فقط یعنی هنوز کسی دقیقاً در آن میلی‌ثانیه نپرسیده است.</p>",
      "example": "<p>دو نشست <code>psql</code> روی پستگرس ۱۸٫۶، با یک جدول دو سطری. حالت اولیه: علی ۵۰۰ و سارا ۱۲۰.</p><p>ترتیب زمانی صریح است — هر بلوک بعد از بلوک بالای خودش اجرا شده. اعلان <code>postgres=#</code> یعنی بیرون تراکنش و <code>postgres=*#</code> یعنی داخل آن.</p><p><strong>گام ۱ — نشست الف</strong> تراکنش را باز می‌کند و نیمهٔ اول انتقال را می‌نویسد:</p><pre><code>postgres=# BEGIN;\nBEGIN\npostgres=*# UPDATE account SET bal = bal - 100 WHERE name = 'ali';\nUPDATE 1</code></pre><p><strong>گام ۲ — نشست ب</strong>، که هیچ ربطی به این انتقال ندارد، همان لحظه می‌پرسد:</p><pre><code>postgres=# SELECT name, bal FROM account ORDER BY name;\n name | bal \n------+-----\n ali  | 500\n sara | 120\n(2 rows)</code></pre><p>مهم‌ترین چیزِ این خروجی، عددی است که <em>نیست</em>. علی هنوز ۵۰۰ است، نه ۴۰۰. و نشست ب معطل هم نشد؛ فوراً پاسخ گرفت.</p><p><strong>گام ۳ — نشست الف</strong> نیمهٔ دوم را می‌نویسد. از دید خودش کار تمام است:</p><pre><code>postgres=*# UPDATE account SET bal = bal + 100 WHERE name = 'sara';\nUPDATE 1</code></pre><p><strong>گام ۴ — نشست ب</strong> دوباره همان پرسش را می‌کند:</p><pre><code>postgres=# SELECT name, bal FROM account ORDER BY name;\n name | bal \n------+-----\n ali  | 500\n sara | 120\n(2 rows)</code></pre><p>هنوز ۵۰۰ و ۱۲۰. تمام کار انجام شده و هیچ ذره‌ای از آن دیده نمی‌شود.</p><p><strong>گام ۵ — نشست الف</strong> تعهد می‌کند:</p><pre><code>postgres=*# COMMIT;\nCOMMIT</code></pre><p><strong>گام ۶ — نشست ب</strong>، همان پرسش، بار سوم:</p><pre><code>postgres=# SELECT name, bal FROM account ORDER BY name;\n name | bal \n------+-----\n ali  | 400\n sara | 220\n(2 rows)</code></pre><p>هر دو سطر با هم عوض شدند. نشست ب سه بار پرسید و دو حالت دید: قبل و بعد. حالت سوم — علی ۴۰۰ و سارا ۱۲۰ — واقعاً وجود داشت و چند میلی‌ثانیه هم طول کشید؛ فقط از بیرون هیچ‌وقت قابل دیدن نبود.</p>"
    },
    "en": {
      "title": "Database Transaction",
      "short": "Several statements bound into one unit — and the main guarantee is not that both happen, it is that the outside never sees only one of them",
      "body": "<p>A hundred thousand tomans from Ali's account to Sara's. For the database this is not one job, it is two: an <code>UPDATE</code> that takes it off Ali and an <code>UPDATE</code> that puts it on Sara.</p><p>The familiar story says: if the power fails between those two, the money evaporates. That is true and it matters — but it is the smallest part of the problem, because power failures are rare.</p><p>What is happening every second is this: between those two statements, for a stretch measured in milliseconds, a hundred thousand tomans is in no account at all. If the total-balance report runs at exactly that moment it gets a number that was never correct and will never be seen again. There is no error, no line in the log, and no way to reproduce it.</p><p>A <strong>database transaction</strong> is the answer to both: it binds several statements into one unit, and that unit is seen from the outside as one step, not several. Either both <code>UPDATE</code>s have happened or neither has, and there is no moment at which a reader on the outside sees one without the other.</p><p><strong>And it is that second clause that the definitions drop.</strong></p><p>\"All or nothing\" is a sentence about the outcome: once the work is finished, one of two states holds. But a reader who asks in the middle does not see the outcome; they see that moment. The second guarantee is about that moment, and the example section shows exactly this: a session that asks mid-transfer does not get the half-done state — it gets the previous state, untouched and correct.</p><p>These two guarantees have separate names, and later stages of this topic open each of them on its own. What to take from here is that there are two of them, not one — and that someone who has only heard the first one does not know half of what a transaction does for them.</p><p><strong>And how the unit is declared.</strong></p><p>With three statements. <code>BEGIN</code> opens the unit, <code>COMMIT</code> says \"done, record it\", and <code>ROLLBACK</code> says \"I changed my mind, let none of it have happened\". Until <code>COMMIT</code> arrives the changes really do exist and the session itself can see them — but for everybody else they do not exist.</p><p>What <code>COMMIT</code> does is called <em>commit</em> throughout this topic, and in the Persian of this site it is «تعهد», deliberately not the word this site already uses for a Git commit. The two are not the same thing, and keeping them apart is cheaper here than forty entries later.</p><p><strong>And a misunderstanding worth removing early: a transaction is not a lock.</strong></p><p>Opening a transaction by itself closes nothing to anybody. The things you do <em>inside</em> it may take locks, but <code>BEGIN</code> itself takes none and makes no other session wait. In the example section the second session gets its answer instantly in the middle of the first session's transaction: it is not blocked, it sees no error, and it has no idea a transaction is open at all.</p><p><strong>And a name collision the reader should know about.</strong></p><p>On this site the id <span dir=\"ltr\">transaction</span> already has an owner: the Bitcoin transaction, in the crypto topic. One word, two completely different things — there it is a signed message broadcast to a network, where \"I sent it\" means \"it started\"; here it is a unit of work inside one engine, where \"I sent it\" means nothing at all. Hence this one's id is <span dir=\"ltr\">db-transaction</span>. You will find the other one among the related terms at the bottom of this page.</p><p><strong>And the last thing that works in review.</strong></p><p>Wherever you find two write statements next to each other in code, ask one question: if the program dies exactly between these two, what state is the database left in and who sees that state? If the answer is \"I don't know\", those two statements are not yet one unit — and the fact that nothing has broken so far only means nobody has yet asked in exactly that millisecond.</p>",
      "example": "<p>Two <code>psql</code> sessions against PostgreSQL 18.6, with a two-row table. Starting state: Ali 500, Sara 120.</p><p>The ordering is explicit — each block ran after the block above it. The prompt <code>postgres=#</code> means outside a transaction and <code>postgres=*#</code> means inside one.</p><p><strong>Step 1 — session A</strong> opens the transaction and writes the first half of the transfer:</p><pre><code>postgres=# BEGIN;\nBEGIN\npostgres=*# UPDATE account SET bal = bal - 100 WHERE name = 'ali';\nUPDATE 1</code></pre><p><strong>Step 2 — session B</strong>, which has nothing to do with this transfer, asks at that moment:</p><pre><code>postgres=# SELECT name, bal FROM account ORDER BY name;\n name | bal \n------+-----\n ali  | 500\n sara | 120\n(2 rows)</code></pre><p>The most important thing in this output is the number that is <em>not</em> there. Ali is still 500, not 400. And session B did not wait either; it answered at once.</p><p><strong>Step 3 — session A</strong> writes the second half. From its own point of view the work is done:</p><pre><code>postgres=*# UPDATE account SET bal = bal + 100 WHERE name = 'sara';\nUPDATE 1</code></pre><p><strong>Step 4 — session B</strong> asks the same question again:</p><pre><code>postgres=# SELECT name, bal FROM account ORDER BY name;\n name | bal \n------+-----\n ali  | 500\n sara | 120\n(2 rows)</code></pre><p>Still 500 and 120. All of the work has happened and not a grain of it is visible.</p><p><strong>Step 5 — session A</strong> commits:</p><pre><code>postgres=*# COMMIT;\nCOMMIT</code></pre><p><strong>Step 6 — session B</strong>, the same question, a third time:</p><pre><code>postgres=# SELECT name, bal FROM account ORDER BY name;\n name | bal \n------+-----\n ali  | 400\n sara | 220\n(2 rows)</code></pre><p>Both rows changed together. Session B asked three times and saw two states: before and after. The third state — Ali 400 and Sara 120 — really did exist and lasted a few milliseconds; it was simply never visible from outside.</p>"
    }
  },
  {
    "id": "transaction-boundary",
    "tags": [
      "transaction",
      "structure"
    ],
    "related": [
      "db-transaction",
      "autocommit",
      "acid"
    ],
    "fa": {
      "title": "مرز تراکنش",
      "short": "آن واحد تا کجاست: چه کسی مرز را می‌کشد، چرا پایگاه داده در این تصمیم دخالتی ندارد، و چرا بازگردانی یک پایان عادی است نه یک خطا",
      "body": "<p>سرویس سفارش سه کار می‌کند: سطر سفارش را می‌نویسد، از موجودی انبار کم می‌کند، و به درگاه پرداخت وصل می‌شود. پرسشی که در بازبینی کد می‌آید همیشه یک شکل دارد — <code>BEGIN</code> کجا بیفتد و <code>COMMIT</code> کجا.</p><p>و نکتهٔ آزاردهنده این است که همهٔ پاسخ‌های ممکن کار می‌کنند. هیچ‌کدام خطا نمی‌دهند، هیچ‌کدام در تست شکست نمی‌خورند، و تفاوتشان فقط زیر بار یا فقط وسط یک خرابی دیده می‌شود.</p><p><strong>مرز تراکنش</strong> <span dir=\"ltr\">(Transaction Boundary)</span> یعنی دقیقاً کدام دستورها داخل یک واحدند و کدام بیرون. مدخل تراکنش پایگاه داده گفت که آن واحد از بیرون یک قدم دیده می‌شود؛ اینجا معلوم می‌شود آن قدم را چه کسی می‌کشد و تا کجا.</p><p><strong>و اولین چیزی که باید روشن باشد: پایگاه داده مرز را نمی‌کشد.</strong></p><p>موتور فقط <code>BEGIN</code> و <code>COMMIT</code> را می‌شنود و هرچه بین آن دو بیاید را یک واحد می‌گیرد. هیچ‌جای پستگرس چیزی نیست که تشخیص دهد این سه <code>UPDATE</code> منطقاً به هم مربوط‌اند و آن یکی نیست. مرز یک تصمیم برنامه است، در کد نوشته می‌شود، و مثل هر تصمیم دیگری می‌تواند غلط باشد بی‌آنکه چیزی اعتراض کند.</p><p>و بی‌طرف بودن پایگاه داده در این تصمیم، معنای دومی هم دارد: اگر لایه‌ای بالای کدت خودش <code>BEGIN</code> می‌زند — یک نگاشتگر شیء-رابطه‌ای <span dir=\"ltr\">(ORM)</span>، یک چارچوب وب، یک صفت روی تابع — مرز را <em>آن</em> کشیده، نه تو. جای پیدا کردن پاسخِ «مرز کجاست» پیکربندی همان لایه است، نه پایگاه داده.</p><p><strong>و دو غلط رایج، که دو سر یک طیف‌اند.</strong></p><p>مرز خیلی تنگ: هر دستور، تراکنش خودش. آن‌وقت هیچ قاعده‌ای که به بیش از یک سطر مربوط است محافظت نمی‌شود، چون هیچ‌وقت دو سطر با هم عوض نمی‌شوند. این حالت نه استثناست و نه نشانهٔ بی‌دقتی؛ حالت پیش‌فرض است، و مدخل بعدیِ همین مرحله تمامش را باز می‌کند.</p><p>مرز خیلی گشاد: یک تماس شبکه‌ای داخل مرز. تراکنش به اندازهٔ کندترین چیزی که داخلش هست زنده می‌ماند، و اگر آن چیز درگاه پرداخت باشد، طول مرز تو را سروری تعیین می‌کند که مال تو نیست. هر قفلی که آن تراکنش گرفته، تمام آن مدت نگه داشته می‌شود.</p><p>قاعدهٔ عملی‌ای که هر دو را می‌گیرد: داخل مرز فقط چیزی باشد که پایگاه داده بتواند پسش بگیرد. یک <code>UPDATE</code> را می‌تواند؛ ایمیلی که فرستاده شده را نمی‌تواند.</p><p><strong>و حالا <code>ROLLBACK</code>، که بیشترین سوءتفاهم را دارد.</strong></p><p>بازگردانی <span dir=\"ltr\">(Rollback)</span> در ذهن بیشتر آدم‌ها مسیر خطاست: چیزی خراب شده، پس برمی‌گردیم. ولی <code>ROLLBACK</code> دقیقاً به همان اندازهٔ <code>COMMIT</code> یک پایان عادی است. پاسخ موتور به آن، کلمهٔ <code>ROLLBACK</code> است و تمام — نه خطایی، نه هشداری، نه سطری که کسی صبح در لاگ ببیند و نگران شود.</p><p>و کاربرد سالمش دقیقاً همین است: تراکنش را باز می‌کنی، تغییر را اعمال می‌کنی، نتیجه را <em>داخل</em> همان تراکنش می‌خوانی، و اگر نتیجه قاعده‌ای را می‌شکند برش می‌گردانی. این «مدیریت خطا» نیست؛ این استفاده از تراکنش به‌عنوان فضایی است که پس گرفتنش رایگان است. بخش مثال همین را نشان می‌دهد: برداشتی که موجودی را منفی می‌کند، دیده شدنِ آن عدد منفی از داخل تراکنش، و بعد بازگردانی.</p><p><strong>و یک نشانهٔ کوچک که مرز را دیدنی می‌کند.</strong></p><p>خودِ <code>psql</code> در اعلانش می‌گوید کجایی: بیرون تراکنش <code>postgres=#</code> و داخل تراکنش <code>postgres=*#</code>. آن ستاره تنها چیزی است که در مثال زیر عوض می‌شود، و تمام تفاوت را نشان می‌دهد. هر کلاینت دیگری نشانهٔ خودش را دارد یا اصلاً ندارد — و «اصلاً ندارد» یعنی تنها راه دانستن اینکه مرزی باز است، خواندن کد است.</p><p><strong>و آخرین چیزی که در بازبینی جواب می‌دهد.</strong></p><p>به‌جای «اینجا تراکنش لازم است؟» دو پرسش بپرس. اول: اگر برنامه بین این دو سطر بمیرد، چه چیزی نصفه می‌ماند؟ این پرسش مرز را باز می‌کند. دوم: داخل این مرز چیزی هست که پایگاه داده نتواند پسش بگیرد؟ این پرسش جلوی گشاد شدنش را می‌گیرد. تقریباً هر مرز بدی که دیده‌ای، از نپرسیدن یکی از این دو آمده.</p>",
      "example": "<p>یک نشست <code>psql</code> روی پستگرس ۱۸٫۶. علی ۴۰۰ دارد و برداشتی هزارتومانی درخواست شده. قاعدهٔ کسب‌وکار می‌گوید موجودی نباید منفی شود، ولی هیچ قیدی در پایگاه داده این را اعلام نکرده — پس برنامه خودش باید تصمیم بگیرد. تغییر را می‌دهد، نتیجه را می‌خواند، و بعد تصمیم می‌گیرد:</p><pre><code>postgres=# BEGIN;\nBEGIN\npostgres=*# UPDATE account SET bal = bal - 1000 WHERE name = 'ali';\nUPDATE 1\npostgres=*# SELECT name, bal FROM account WHERE name = 'ali';\n name | bal  \n------+------\n ali  | -600\n(1 row)\n\npostgres=*# ROLLBACK;\nROLLBACK\npostgres=# SELECT name, bal FROM account WHERE name = 'ali';\n name | bal \n------+-----\n ali  | 400\n(1 row)</code></pre><p>چهار چیز در این خروجی خواندنی است.</p><p>ستاره در اعلان از دستور دوم تا خودِ <code>ROLLBACK</code> هست و بعدش نیست. این همان مرز است، دیدنی.</p><p>سطر <code>-600</code> واقعی است. تراکنش تغییر خودش را می‌بیند، حتی تغییری را که قرار است هیچ‌وقت نماند. همین است که این الگو را ممکن می‌کند: تصمیم بر اساس نتیجه گرفته می‌شود، نه بر اساس پیش‌بینی نتیجه.</p><p>پاسخ پستگرس به <code>ROLLBACK</code> فقط کلمهٔ <code>ROLLBACK</code> است. نه <code>ERROR</code>، نه <code>WARNING</code>. این خروجیِ یک عملیات موفق است.</p><p>و آخرین <code>SELECT</code> همان ۴۰۰ اول را می‌دهد. نه اثری مانده و نه ردی.</p><p>و برای اینکه معلوم شود مرز یک حالت است نه یک آیین، همین <code>COMMIT</code> را بیرون هر مرزی امتحان کن:</p><pre><code>postgres=# COMMIT;\nWARNING:  there is no transaction in progress\nCOMMIT</code></pre><p>هشدار می‌دهد و بعد باز هم <code>COMMIT</code> را چاپ می‌کند. خطایی رخ نداده؛ فقط چیزی برای تعهد کردن نبوده.</p>"
    },
    "en": {
      "title": "Transaction Boundary",
      "short": "How far the unit reaches: who draws the boundary, why the database has no say in it, and why a rollback is a normal ending rather than an error",
      "body": "<p>The order service does three things: it writes the order row, it takes stock off the warehouse count, and it calls the payment gateway. The question that comes up in review always has the same shape — where does <code>BEGIN</code> go and where does <code>COMMIT</code>.</p><p>And the annoying part is that every possible answer works. None of them errors, none of them fails a test, and the difference between them only shows up under load or in the middle of a failure.</p><p>The <strong>transaction boundary</strong> is exactly which statements are inside one unit and which are outside. The database transaction entry said that the unit is seen from outside as one step; this is about who draws that step, and how far.</p><p><strong>And the first thing to be clear about: the database does not draw the boundary.</strong></p><p>The engine hears only <code>BEGIN</code> and <code>COMMIT</code> and treats whatever arrives between them as one unit. There is nothing anywhere in PostgreSQL that works out that these three <code>UPDATE</code>s logically belong together and that one does not. The boundary is a decision made by the program, written in code, and like any other decision it can be wrong without anything objecting.</p><p>The database's neutrality here has a second consequence: if a layer above your code issues the <code>BEGIN</code> — an ORM, a web framework, an annotation on a method — then <em>it</em> drew the boundary, not you. The place to find the answer to \"where is the boundary\" is that layer's configuration, not the database.</p><p><strong>And two common mistakes, which are the two ends of one spectrum.</strong></p><p>Too narrow: every statement its own transaction. Then no rule that spans more than one row is ever protected, because two rows never change together. This is not an exception and not a sign of carelessness; it is the default, and the next entry in this stage opens all of it.</p><p>Too wide: a network call inside the boundary. A transaction stays alive as long as the slowest thing inside it, and if that thing is a payment gateway then the length of your boundary is set by a server that is not yours. Every lock that transaction took is held for the whole of it.</p><p>The practical rule that catches both: only put things inside the boundary that the database can take back. It can take back an <code>UPDATE</code>; it cannot take back an email that has been sent.</p><p><strong>And now <code>ROLLBACK</code>, which is the most misunderstood of the three.</strong></p><p>Most people file rollback under error handling: something broke, so we go back. But <code>ROLLBACK</code> is exactly as normal an ending as <code>COMMIT</code>. The engine's answer to it is the word <code>ROLLBACK</code> and nothing else — no error, no warning, no line that somebody finds in the log in the morning and worries about.</p><p>And its healthy use is precisely this: open the transaction, apply the change, read the result <em>inside</em> that same transaction, and if the result breaks a rule, roll it back. That is not error handling; that is using a transaction as a space where undoing is free. The example section shows it: a withdrawal that drives the balance negative, that negative number seen from inside the transaction, and then a rollback.</p><p><strong>And one small sign that makes the boundary visible.</strong></p><p><code>psql</code> tells you where you are in its own prompt: outside a transaction it is <code>postgres=#</code> and inside one it is <code>postgres=*#</code>. That asterisk is the only thing that changes in the example below, and it shows the whole difference. Every other client has its own sign or none at all — and \"none at all\" means the only way to know a boundary is open is to read the code.</p><p><strong>And the last thing that works in review.</strong></p><p>Instead of \"do we need a transaction here?\", ask two questions. First: if the program dies between these two lines, what is left half done? That question opens the boundary. Second: is there anything inside this boundary that the database cannot take back? That question stops it growing. Nearly every bad boundary you have seen came from not asking one of those two.</p>",
      "example": "<p>One <code>psql</code> session against PostgreSQL 18.6. Ali has 400 and a withdrawal of 1000 has been requested. The business rule says the balance must not go negative, but no constraint in the database declares that — so the program has to decide for itself. It makes the change, reads the result, and then decides:</p><pre><code>postgres=# BEGIN;\nBEGIN\npostgres=*# UPDATE account SET bal = bal - 1000 WHERE name = 'ali';\nUPDATE 1\npostgres=*# SELECT name, bal FROM account WHERE name = 'ali';\n name | bal  \n------+------\n ali  | -600\n(1 row)\n\npostgres=*# ROLLBACK;\nROLLBACK\npostgres=# SELECT name, bal FROM account WHERE name = 'ali';\n name | bal \n------+-----\n ali  | 400\n(1 row)</code></pre><p>Four things in this output are worth reading.</p><p>The asterisk in the prompt is there from the second statement up to and including <code>ROLLBACK</code>, and gone afterwards. That is the boundary, made visible.</p><p>The <code>-600</code> row is real. A transaction sees its own change, including a change that is never going to last. That is what makes this pattern possible: the decision is made on the result, not on a prediction of the result.</p><p>PostgreSQL's answer to <code>ROLLBACK</code> is the word <code>ROLLBACK</code>. Not <code>ERROR</code>, not <code>WARNING</code>. That is the output of a successful operation.</p><p>And the final <code>SELECT</code> gives back the same 400 it started with. No effect left, and no trace.</p><p>And to show that a boundary is a state rather than a ritual, try that same <code>COMMIT</code> outside any boundary at all:</p><pre><code>postgres=# COMMIT;\nWARNING:  there is no transaction in progress\nCOMMIT</code></pre><p>It warns, and then prints <code>COMMIT</code> anyway. Nothing failed; there was simply nothing to commit.</p>"
    }
  },
  {
    "id": "autocommit",
    "tags": [
      "transaction",
      "decision"
    ],
    "related": [
      "transaction-boundary",
      "db-transaction",
      "acid"
    ],
    "fa": {
      "title": "تعهد خودکار",
      "short": "دستوری که آن را در هیچ تراکنشی نپیچیده‌ای بی‌تراکنش نیست؛ خودش یک تراکنش تک‌دستوری است — پیش‌فرضی که هم بیشتر از انتظار می‌دهد و هم درست همان‌جا که لازم است کم می‌آورد",
      "body": "<p>پرسشی که بارها در بازبینی کد می‌آید: «این <code>UPDATE</code> تنهاست؛ محض احتیاط داخل تراکنش بگذارمش؟»</p><p>از قبل داخل تراکنش است.</p><p>و پاسخ بلندترش این است که اصلاً راهی وجود ندارد که یک دستور <span dir=\"ltr\">SQL</span> بیرون از تراکنش اجرا شود. اگر <code>BEGIN</code> ننوشته باشی، موتور خودش یکی باز می‌کند، دستور را اجرا می‌کند، و بلافاصله تعهدش می‌کند. این حالت <strong>تعهد خودکار</strong> <span dir=\"ltr\">(Autocommit)</span> نام دارد و پیش‌فرض است.</p><p><strong>و جای این تنظیم، جای عجیبی است.</strong></p><p>اگر از خود سرور بپرسی، پاسخی ندارد: روی پستگرس ۱۸٫۶ دستور <code>SHOW autocommit</code> با «پارامتر پیکربندی ناشناخته» رد می‌شود. تعهد خودکار اصلاً تنظیمی روی پایگاه داده نیست. در <code>psql</code> یک متغیرِ خودِ <code>psql</code> است که مقدارش <code>on</code> است.</p><p>و این ریزه‌کاری بی‌فایده‌ای نیست، چون جای پاسخ را عوض می‌کند. وقتی می‌پرسی «آیا این اتصال تعهد خودکار دارد؟» جواب را باید در تنظیم اتصالِ همان کتابخانه یا همان لایه‌ای پیدا کنی که اتصال را ساخته، نه در پیکربندی پایگاه داده. یعنی دو سرویس که به یک پایگاه دادهٔ واحد وصل‌اند می‌توانند دو رفتار متفاوت داشته باشند و هیچ‌چیز در آن پایگاه داده این تفاوت را نشان نمی‌دهد.</p><p><strong>و حالا آنچه این پیش‌فرض <em>می‌دهد</em>، که معمولاً دست‌کم گرفته می‌شود.</strong></p><p>یک <code>UPDATE</code> تنها از قبل اتمی است. اگر هزار سطر را عوض کند و روی سطر پانصدم قیدی بشکند، هیچ‌کدام از آن هزار سطر عوض نمی‌شود — نه چهارصد و نود و نه‌تا. بخش مثال همین را روی سه سطر نشان می‌دهد: سطر اول بی‌اشکال محاسبه می‌شود، سطر دوم قید را می‌شکند، و بعدش هر سه سطر همان‌اند که بودند.</p><p>و از همان لحظه‌ای که <code>UPDATE 1</code> را دیدی، آن تغییر دیگر به زنده بودن اتصالی که نوشتش وابسته نیست. بخش مثال این را با کشتن اتصال نشان می‌دهد. پایداری <span dir=\"ltr\">(Durability)</span> در برابر قطع برق چیز دیگری است و مرحلهٔ دهم این مسیر تمامش را باز می‌کند؛ آنچه اینجا اندازه گرفته شده کمتر از آن است — و از قضا همان است که در عمل بارها بیشتر پیش می‌آید: اتصالی که می‌میرد.</p><p><strong>و حالا آنچه <em>نمی‌دهد</em>.</strong></p><p>مدخل مرز تراکنش گفت که مرز را برنامه می‌کشد. تعهد خودکار یعنی وقتی برنامه مرزی نکشیده، باز هم مرزی هست — و آن مرز دورِ <em>هر دستور، جداگانه</em> کشیده شده. یعنی دو دستوری که منطقاً به هم مربوط‌اند، دو واحدند، و بین آن دو یک لحظهٔ کاملاً دیدنی وجود دارد.</p><p>این همان لحظه‌ای است که در انتقال پول، صد هزار تومان در هیچ حسابی نبود. تعهد خودکار آن لحظه را نمی‌سازد و حذف هم نمی‌کند؛ فقط باعث می‌شود کسی متوجه وجودش نشود، چون هیچ‌جای کد کلمه‌ای نوشته نشده که آدم را به فکر بیندازد. یک مرز غلط دست‌کم دیده می‌شود؛ مرزی که پیش‌فرض کشیده، نه.</p><p><strong>و شمارشی که ارزش نگه داشتن دارد.</strong></p><p>حلقه‌ای که هزار سطر را یکی‌یکی درج می‌کند، هزار تراکنش است و هزار تعهد — نه یکی. این نه حدس است و نه تفسیر؛ مستقیماً از تعریف تعهد خودکار درمی‌آید. اینکه هزار تعهد چقدر بیشتر از یکی هزینه دارد و چرا، پرسش مرحلهٔ سوم این مسیر است.</p><p><strong>و آخرین چیزی که در بازبینی جواب می‌دهد.</strong></p><p>هرجا جمله‌ای شنیدی به شکل «ما اینجا از تراکنش استفاده نمی‌کنیم»، ترجمه‌اش این است: «ما اینجا هر دستور را یک تراکنش جداگانه کرده‌ایم». این یک تصمیم است، نه نبودِ تصمیم — و مثل هر تصمیمی، باید بشود گفت چرا.</p>",
      "example": "<p>نشستی روی پستگرس ۱۸٫۶. اول از خود سرور می‌پرسیم تعهد خودکار کجاست، بعد یک <code>UPDATE</code> بدون <code>BEGIN</code> می‌زنیم. سارا ۲۲۰ دارد:</p><pre><code>postgres=# SHOW autocommit;\nERROR:  unrecognized configuration parameter \"autocommit\"\npostgres=# \\echo :AUTOCOMMIT\non\npostgres=# UPDATE account SET bal = bal + 50 WHERE name = 'sara';\nUPDATE 1</code></pre><p>سه چیز اینجا هست. سرور چنین پارامتری نمی‌شناسد. <code>psql</code> متغیر خودش را دارد و روشن است. و بعد از <code>UPDATE 1</code> اعلان هنوز <code>postgres=#</code> است، بدون ستاره — یعنی چیزی باز نمانده که بعداً بسته شود.</p><p>حالا این اتصال از بیرون کشته می‌شود؛ نه <code>\\q</code>، بلکه پایان دادن به همان نشست از یک اتصال دیگر. بعد یک نشست کاملاً تازه باز می‌کنیم و می‌پرسیم:</p><pre><code>postgres=# SELECT name, bal FROM account WHERE name = 'sara';\n name | bal \n------+-----\n sara | 270\n(1 row)</code></pre><p>۲۲۰ رفته به ۲۷۰ و مانده. هیچ‌کس <code>COMMIT</code> ننوشت.</p><p>حالا همان دستور، این بار داخل یک مرز صریح:</p><pre><code>postgres=# BEGIN;\nBEGIN\npostgres=*# UPDATE account SET bal = bal + 50 WHERE name = 'sara';\nUPDATE 1</code></pre><p>ستاره برگشت. دوباره همان کشتن از بیرون، و دوباره یک نشست تازه:</p><pre><code>postgres=# SELECT name, bal FROM account WHERE name = 'sara';\n name | bal \n------+-----\n sara | 270\n(1 row)</code></pre><p>۲۷۰ همان ۲۷۰ است. آن ۵۰ تومان دوم هیچ‌وقت نبوده. تفاوت این دو آزمایش یک کلمه است: <code>BEGIN</code>.</p><p>و اتمی بودنِ یک دستور تنها، که با یک قید ساده دیدنی می‌شود:</p><pre><code>postgres=# CREATE TABLE t (n int CHECK (n < 100));\nCREATE TABLE\npostgres=# INSERT INTO t VALUES (1),(2),(3);\nINSERT 0 3\npostgres=# UPDATE t SET n = n * 50;\nERROR:  new row for relation \"t\" violates check constraint \"t_n_check\"\nDETAIL:  Failing row contains (100).\npostgres=# SELECT n FROM t ORDER BY n;\n n \n---\n 1\n 2\n 3\n(3 rows)</code></pre><p><code>UPDATE</code> هر سه سطر را برمی‌داشت. سطر ۱ به ۵۰ می‌رفت و بی‌اشکال بود؛ سطر ۲ به ۱۰۰ می‌رفت و قید «کمتر از صد» را می‌شکست. نتیجه این است که هیچ سطری عوض نشد، حتی سطر اول که محاسبه‌اش موفق بود. یک دستور تنها، یک واحد است.</p>"
    },
    "en": {
      "title": "Autocommit",
      "short": "A statement you did not wrap is not outside a transaction; it is its own single-statement transaction — a default that gives more than people expect and falls short exactly where it matters",
      "body": "<p>A question that comes up in review again and again: \"this <code>UPDATE</code> is on its own; should I wrap it in a transaction to be safe?\"</p><p>It is already in one.</p><p>The longer answer is that there is no way at all for an SQL statement to run outside a transaction. If you did not write <code>BEGIN</code>, the engine opens one itself, runs the statement, and commits it immediately. That mode is called <strong>autocommit</strong>, and it is the default.</p><p><strong>And this setting lives in a strange place.</strong></p><p>Ask the server and it has no answer: on PostgreSQL 18.6 the command <code>SHOW autocommit</code> is rejected as an unrecognized configuration parameter. Autocommit is not a database setting at all. In <code>psql</code> it is a variable of <code>psql</code> itself, and its value is <code>on</code>.</p><p>This is not a useless detail, because it moves where the answer lives. When you ask \"does this connection autocommit?\", you have to find the answer in the connection settings of whatever library or layer built the connection, not in the database's configuration. Which means two services connected to one and the same database can behave differently, and nothing in that database shows the difference.</p><p><strong>And now what this default <em>gives</em> you, which is usually underrated.</strong></p><p>A lone <code>UPDATE</code> is already atomic. If it changes a thousand rows and a constraint breaks on the five-hundredth, none of the thousand rows change — not four hundred and ninety-nine of them. The example section shows this on three rows: the first row computes cleanly, the second breaks the constraint, and afterwards all three rows are as they were.</p><p>And from the moment you see <code>UPDATE 1</code>, that change no longer depends on the connection that wrote it staying alive. The example section shows that by killing the connection. Durability against a power cut is a different thing, and stage ten of this path opens all of it; what is measured here is less than that — and happens to be the case that occurs far more often in practice: a connection that dies.</p><p><strong>And now what it does not give you.</strong></p><p>The transaction boundary entry said the boundary is drawn by the program. Autocommit means that when the program drew no boundary, there is a boundary anyway — and it is drawn around <em>each statement, separately</em>. So two statements that logically belong together are two units, and there is a fully visible moment between them.</p><p>That is the same moment in which, during the transfer, a hundred thousand tomans was in no account. Autocommit neither creates that moment nor removes it; it only makes it hard to notice, because nowhere in the code is there a word that would prompt the thought. A wrong boundary can at least be seen; a boundary the default drew cannot.</p><p><strong>And a count worth keeping.</strong></p><p>A loop that inserts a thousand rows one at a time is a thousand transactions and a thousand commits — not one. That is neither a guess nor an interpretation; it falls straight out of the definition of autocommit. How much more a thousand commits cost than one, and why, is the question stage three of this path asks.</p><p><strong>And the last thing that works in review.</strong></p><p>Wherever you hear a sentence of the form \"we don't use transactions here\", the translation is: \"we have made every statement its own transaction here\". That is a decision, not the absence of one — and like any decision, somebody should be able to say why.</p>",
      "example": "<p>A session against PostgreSQL 18.6. First we ask the server where autocommit lives, then we run an <code>UPDATE</code> with no <code>BEGIN</code>. Sara has 220:</p><pre><code>postgres=# SHOW autocommit;\nERROR:  unrecognized configuration parameter \"autocommit\"\npostgres=# \\echo :AUTOCOMMIT\non\npostgres=# UPDATE account SET bal = bal + 50 WHERE name = 'sara';\nUPDATE 1</code></pre><p>Three things here. The server does not know such a parameter. <code>psql</code> has its own variable and it is on. And after <code>UPDATE 1</code> the prompt is still <code>postgres=#</code>, with no asterisk — nothing is left open to be closed later.</p><p>Now this connection is killed from outside; not <code>\\q</code>, but terminating that session from another connection. Then we open a completely fresh session and ask:</p><pre><code>postgres=# SELECT name, bal FROM account WHERE name = 'sara';\n name | bal \n------+-----\n sara | 270\n(1 row)</code></pre><p>220 has become 270 and stayed. Nobody wrote <code>COMMIT</code>.</p><p>Now the same statement, this time inside an explicit boundary:</p><pre><code>postgres=# BEGIN;\nBEGIN\npostgres=*# UPDATE account SET bal = bal + 50 WHERE name = 'sara';\nUPDATE 1</code></pre><p>The asterisk is back. The same kill from outside again, and a fresh session again:</p><pre><code>postgres=# SELECT name, bal FROM account WHERE name = 'sara';\n name | bal \n------+-----\n sara | 270\n(1 row)</code></pre><p>270 is still 270. That second 50 never existed. The difference between the two experiments is one word: <code>BEGIN</code>.</p><p>And the atomicity of a lone statement, made visible with one simple constraint:</p><pre><code>postgres=# CREATE TABLE t (n int CHECK (n < 100));\nCREATE TABLE\npostgres=# INSERT INTO t VALUES (1),(2),(3);\nINSERT 0 3\npostgres=# UPDATE t SET n = n * 50;\nERROR:  new row for relation \"t\" violates check constraint \"t_n_check\"\nDETAIL:  Failing row contains (100).\npostgres=# SELECT n FROM t ORDER BY n;\n n \n---\n 1\n 2\n 3\n(3 rows)</code></pre><p>The <code>UPDATE</code> was taking all three rows. Row 1 was going to 50 and was fine; row 2 was going to 100 and broke the \"less than a hundred\" constraint. The result is that no row changed, not even the first one whose computation succeeded. A lone statement is one unit.</p>"
    }
  },
  {
    "id": "acid",
    "tags": [
      "transaction",
      "decision"
    ],
    "related": [
      "db-transaction",
      "transaction-boundary",
      "autocommit"
    ],
    "fa": {
      "title": "ACID",
      "short": "چهار حرف که چهار ضمانت متفاوت‌اند، هرکدام دربارهٔ یک تراکنش — و فهرست چیزهایی که هیچ‌کدامشان دربارهٔ آن‌ها حرفی نمی‌زند، از خود چهار حرف طولانی‌تر است",
      "body": "<p>روی صفحهٔ محصولِ تقریباً هر پایگاه دادهٔ رابطه‌ای یک عبارت هست: «کاملاً <span dir=\"ltr\">ACID</span>». آن‌قدر تکرار شده که دیگر مثل یک مهر کیفیت خوانده می‌شود تا مثل ادعایی مشخص.</p><p>ولی ادعایی مشخص است، و همین مشخص بودن است که به کار می‌آید — چون هر چهار حرفش دربارهٔ یک چیز حرف می‌زنند: یک تراکنش. نه دربارهٔ پایگاه داده، نه دربارهٔ برنامه، و نه دربارهٔ سیستم.</p><p>سرنام را تئو هردر و آندریاس روتر در ۱۹۸۳ در مقاله‌ای مروری ساختند، روی مفهومی که پیش از آن جیم گری صورت‌بندی کرده بود. یعنی این چهار حرف از اول هم توصیفِ یک دستهٔ ضمانت بودند، نه فهرست ویژگی‌های یک محصول.</p><p>اینجا فقط نام‌گذاری می‌شوند. هرکدام مرحله‌ای از این مسیر را برای خودش دارد؛ این یکی نقشه است، نه شرح.</p><p><strong>A — اتمی بودن</strong> <span dir=\"ltr\">(Atomicity)</span>. یا همهٔ دستورهای تراکنش اثر می‌کنند یا هیچ‌کدام. مرحلهٔ دوم این مسیر این حرف را باز می‌کند و نشان می‌دهد که «پس گرفتن کار» با «انجام ندادنش» یکی نیست: برای پس گرفتن، موتور باید از قبل چیزی نگه داشته باشد.</p><p><strong>C — سازگاری</strong> <span dir=\"ltr\">(Consistency)</span>. تراکنش پایگاه داده را از یک حالت معتبر به حالت معتبر دیگری می‌برد؛ یعنی قیدهایی که خودت اعلام کرده‌ای، بعد از تعهد هم برقرارند. مرحلهٔ چهارم این حرف را باز می‌کند و استدلال می‌کند که این یکی اصلاً از جنس آن سه‌تای دیگر نیست.</p><p><strong>I — جداسازی</strong> <span dir=\"ltr\">(Isolation)</span>. تراکنش‌هایی که هم‌زمان اجرا می‌شوند طوری نتیجه می‌دهند که انگار پشت سر هم اجرا شده‌اند. این پرحرف‌ترین حرف است و سه مرحله از این مسیر خرجش می‌شود، چون در عمل درجه دارد و درجه‌اش را تو انتخاب می‌کنی.</p><p><strong>D — پایداری</strong> <span dir=\"ltr\">(Durability)</span>. وقتی تعهد اعلام شد، اثرش می‌ماند، حتی اگر بلافاصله بعدش برق برود. مرحلهٔ دهم این حرف را باز می‌کند و این پرسش را که «ماند» دقیقاً یعنی کجا.</p><p><strong>و حالا مهم‌ترین بخش این نقشه: آنچه چهار حرف نمی‌گویند.</strong></p><p>هیچ‌کدام دربارهٔ دسترسی حرفی نمی‌زنند. تراکنشی که با نشست یک کاربر، حساب کاربر دیگری را خالی می‌کند می‌تواند کاملاً اتمی، سازگار، جدا و پایدار باشد. چهار حرف در آن حالت تضمین می‌کنند که این خالی کردن کامل و ماندگار انجام شود.</p><p>هیچ‌کدام دربارهٔ درست بودن کد تو حرفی نمی‌زنند. اگر منطق کسب‌وکار اشتباه باشد، <span dir=\"ltr\">ACID</span> با کمال دقت همان اشتباه را ثبت می‌کند. حرف <span dir=\"ltr\">C</span> فقط قاعده‌هایی را نگه می‌دارد که به پایگاه داده <em>گفته‌ای</em>؛ قاعده‌ای که در ذهن تو یا در یک سند است، برای موتور اصلاً وجود ندارد.</p><p>هیچ‌کدام دربارهٔ مقیاس حرفی نمی‌زنند. <span dir=\"ltr\">ACID</span> نه عددی دربارهٔ توان عملیاتی می‌دهد، نه دربارهٔ تأخیر، و نه دربارهٔ تعداد ماشین‌ها. «کاملاً <span dir=\"ltr\">ACID</span>» و «کند» هیچ تناقضی با هم ندارند، و همین‌طور «کاملاً <span dir=\"ltr\">ACID</span>» و «تک‌ماشینه».</p><p>و هیچ‌کدام دربارهٔ فاصلهٔ میان دو تراکنش حرفی نمی‌زنند. تضمین‌ها دربارهٔ یک واحد کارند. اگر برنامه‌ات کاری را که باید یک واحد می‌بود به سه واحد شکسته باشد، هر سه واحد کاملاً <span dir=\"ltr\">ACID</span> اجرا می‌شوند و نتیجه همچنان غلط است. مدخل مرز تراکنش دربارهٔ همین شکستن است.</p><p><strong>و یک پرسش که این عبارت را از حالت مهر کیفیت درمی‌آورد.</strong></p><p>وقتی چیزی می‌گوید «کاملاً <span dir=\"ltr\">ACID</span>»، بپرس «در کدام سطح جداسازی، به‌طور پیش‌فرض؟». پستگرس ۱۸٫۶ اگر از آن بپرسی، <span dir=\"ltr\">read committed</span> جواب می‌دهد — نه بالاترین سطحی که بلد است. این کم‌فروشی نیست؛ انتخابی است که سازنده به‌جای تو کرده و می‌شود عوضش کرد. مرحله‌های هفتم و هشتم این مسیر تماماً دربارهٔ همین‌اند، و مرحلهٔ هشتم نشان می‌دهد که یک نام واحد روی دو موتور مختلف دو رفتار مختلف دارد.</p><p><strong>و آخرین چیزی که در بازبینی جواب می‌دهد.</strong></p><p>مدخل تراکنش پایگاه داده گفت که تراکنش یک واحد کار است. <span dir=\"ltr\">ACID</span> فهرست چیزهایی است که دربارهٔ آن واحد تضمین می‌شود — و درست به همین دلیل، هرچه واحد نیست بیرون این چهار حرف است. پس هر بار که این عبارت را جایی دیدی، اولین پرسش این است: تضمین دربارهٔ کدام واحد؟</p>"
    },
    "en": {
      "title": "ACID",
      "short": "Four letters that are four different guarantees, each about a single transaction — and the list of things none of them says anything about is longer than the four",
      "body": "<p>On the product page of nearly every relational database there is a phrase: \"fully ACID\". It has been repeated so often that it now reads like a quality stamp rather than a specific claim.</p><p>But it is a specific claim, and its specificity is exactly what makes it useful — because all four letters are about one thing: a transaction. Not about the database, not about the application, and not about the system.</p><p>The acronym was coined by Theo Härder and Andreas Reuter in a 1983 survey paper, on top of a notion Jim Gray had formulated before that. So the four letters were a description of a class of guarantees from the start, not a feature list for a product.</p><p>Here they are only named. Each of them has a stage of this path to itself; this one is a map, not an explanation.</p><p><strong>A — Atomicity.</strong> Either all the statements of a transaction take effect or none of them do. Stage two of this path opens this letter and shows that \"taking work back\" is not the same as \"not doing it\": to take it back, the engine must have kept something beforehand.</p><p><strong>C — Consistency.</strong> A transaction moves the database from one valid state to another valid state; that is, the constraints you declared still hold after the commit. Stage four opens this letter and argues that this one is not of the same kind as the other three at all.</p><p><strong>I — Isolation.</strong> Transactions running at the same time produce a result as if they had run one after another. This is the talkative letter — three stages of this path go on it — because in practice it has degrees, and you choose the degree.</p><p><strong>D — Durability.</strong> Once a commit has been reported, its effect stays, even if the power goes out immediately afterwards. Stage ten opens this letter, and the question of where exactly \"stays\" is.</p><p><strong>And now the most important part of this map: what the four letters do not say.</strong></p><p>None of them says anything about access. A transaction that uses one user's session to empty another user's account can be perfectly atomic, consistent, isolated and durable. In that case the four letters guarantee that the emptying is done completely and permanently.</p><p>None of them says anything about your code being right. If the business logic is wrong, ACID will record that exact mistake with great care. The C only holds the rules you have <em>told</em> the database; a rule that lives in your head or in a document does not exist for the engine at all.</p><p>None of them says anything about scale. ACID gives no number for throughput, none for latency, and none for how many machines. \"Fully ACID\" and \"slow\" are not in contradiction, and neither are \"fully ACID\" and \"single machine\".</p><p>And none of them says anything about the gap between two transactions. The guarantees are about one unit of work. If your program has broken into three units something that should have been one, all three units run perfectly ACID and the result is still wrong. The transaction boundary entry is about exactly that breaking.</p><p><strong>And one question that takes the phrase out of quality-stamp mode.</strong></p><p>When something says \"fully ACID\", ask \"at which isolation level, by default?\". Ask PostgreSQL 18.6 and it answers <span dir=\"ltr\">read committed</span> — not the strongest level it knows. That is not a short measure; it is a choice the vendor made on your behalf, and it can be changed. Stages seven and eight of this path are entirely about this, and stage eight shows that one and the same level name means two different behaviours on two different engines.</p><p><strong>And the last thing that works in review.</strong></p><p>The database transaction entry said a transaction is a unit of work. ACID is the list of things guaranteed about that unit — and for exactly that reason, whatever is not the unit is outside the four letters. So every time you see the phrase, the first question is: guaranteed about which unit?</p>"
    }
  }
]
